# -*- mode: snippet -*-
# name: haskell
# key: main
# --
{ pkgs ? import <nixpkgs> { } }:
with pkgs;
let ghc = haskellPackages.ghcWithPackages (ps: [ ps.cabal-fmt ]);
in mkShell {
  buildInputs = [ ghcid ghc cabal-install haskell-language-server ormolu ];
  ENV_VAR = "env_var";
}
