[general]
accounts = User
maxsyncaccounts = 3
pythonfile = ~/.offlineimap.py

[Account User]
remoterepository = UserRemote
localrepository = UserLocal

[Repository UserRemote]
type = IMAP
ssl = yes
sslcacertfile = /etc/ssl/certs/ca-certificates.crt
remotehost = xxx.com
remoteport = 1993
remoteuser = xxx
#remotepass = xxx
remotepasseval = get_password_emacs("xxx", "yyy", "1587")
folderfilter = lambda foldername: foldername not in ['Calendar', 'Contacts']

# If you don't want local changes to sync back to the server (e.g. deleting
# mail in the maildir will delete them on the server), uncomment this line:
#readonly = yes

[Repository UserLocal]
type = Maildir
localfolders = ~/Mail/User