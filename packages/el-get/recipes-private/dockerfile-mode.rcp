(:name dockerfile-mode
       :description "An emacs mode for handling Dockerfiles"
       :type github
       :pkgname "spotify/dockerfile-mode"
       :build `(,(concat "cp *.el "
                         (in-emacs-d "prog_lang/docker")))
       )
