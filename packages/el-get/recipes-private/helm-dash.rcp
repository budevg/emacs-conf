(:name "helm-dash"
       :description "Browse Dash docsets inside emacs"
       :depends helm
       :type github
       :pkgname "areina/helm-dash"
       :build `(,(concat "cp *.el "
                         (in-emacs-d "navigation/helm")))
       )
