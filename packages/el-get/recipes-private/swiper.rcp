(:name swiper
       :description "Gives you an overview as you search for a regex."
       :type github
       :pkgname "abo-abo/swiper"
       :build `(,(concat "cp *.el "
                         (in-emacs-d "navigation/swiper")))
       )
