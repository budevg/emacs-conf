(:name dash
       :description "A modern list api for Emacs. No 'cl required."
       :type github
       :pkgname "magnars/dash.el"
       :build `(,(concat "cp *.el "
                         (in-emacs-d "lisp_lib")))

       )
