(:name hydra
       :description "make Emacs bindings that stick around"
       :type github
       :depends (cl-lib)
       :pkgname "abo-abo/hydra"
       :build `(,(concat "cp *.el "
                         (in-emacs-d "lisp_lib")))

       )
