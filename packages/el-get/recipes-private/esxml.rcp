(:name esxml
       :description "Library for working with xml via esxml and sxml."
       :type github
       :pkgname "tali713/esxml"
       :build `(,(concat "cp *.el "
                         (in-emacs-d "lisp_lib/esxml")))
       )
