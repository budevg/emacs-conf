(:name terraform-mode
       :type github
       :pkgname "syohex/emacs-terraform-mode"
       :description "Major mode for Terraform configuration files"
       :build `(,(concat "cp *.el "
                         (in-emacs-d "prog_lang/hcl")))
       :depends (hcl-mode))
